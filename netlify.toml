[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Installs deps from the function's own package.json (@anthropic-ai/sdk) at build time.
[[plugins]]
  package = "@netlify/plugin-functions-install-core"
